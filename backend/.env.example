# Environment variables declared in this file are automatically made available to Prisma.
# See the documentation for more detail: https://pris.ly/d/prisma-schema#accessing-environment-variables-from-the-schema

# Prisma supports the native connection string format for PostgreSQL, MySQL, SQLite, SQL Server, MongoDB and CockroachDB.
# See the documentation for all the connection string options: https://pris.ly/d/connection-strings

DATABASE_URL="mysql://root@127.0.0.1:3306/prisma_testdb"
FRONTEND_BASE_URL="http://localhost:5173"
APP_STATE="production" # "production" || "development" 
SERVER_PORT=3000

JWT_SECRET=node && require('crypto').randomBytes(64).toString('hex')
JWT_REFRESH_SECRET=node && require('crypto').randomBytes(64).toString('hex')
JWT_ACCESS_TOKEN_EXPIRY="15m"
JWT_REFRESH_TOKEN_EXPIRY="7d"

GMAIL_APP_PASSWORD="abcd efgh ijkl mnop"
NODEMAILER_EMAIL_FROM="johndoe@gmail.com"
GEMINI_API_KEY="qWeRtYiP237w89y23y89YT8wqftyqf"